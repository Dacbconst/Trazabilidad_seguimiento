<?php
/**
 * Tab "Pipeline" — Vista del flujo comercial por PDV.
 * Reutiliza el componente estado-flujo (CSS + JS propios).
 * $cuenta_dir viene del index.php que incluye este partial.
 */
include $cuenta_dir . '/components/estado-flujo/estado-flujo.php';
